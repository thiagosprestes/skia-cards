import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: "#000",
    paddingHorizontal: 20,
    paddingBottom: 20,
  },
  cardContainer: {
    marginTop: 90,
    marginBottom: 40,
    alignItems: "center",
  },
});
