import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  button: {
    backgroundColor: "#fff",
    padding: 12,
    borderRadius: 8,
  },
  buttonText: {
    color: "#000",
    fontSize: 18,
    textAlign: "center",
    fontFamily: "Roboto_700Bold",
  },
  disabled: {
    opacity: 0.5,
  },
  previousButton: {
    backgroundColor: "#000",
    borderWidth: 2,
    borderColor: "#fff",
    marginTop: 10,
  },
  previousButtonText: {
    color: "#fff",
  },
});
